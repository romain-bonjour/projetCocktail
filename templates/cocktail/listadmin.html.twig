{% extends 'base.html.twig' %}

{% block title %} Modification des cocktails {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Modifications des Cocktails</h1>

    Vous trouverez si dessous tout vos coktail préféré et bien plus encore 

    <ul>
        {% for cocktail in cocktails %}
            <li>{{ cocktail.label }} , {{ cocktail.description }} , {{ cocktail.label }}
            {% if cocktail.active %}
                Est actifs
            {% else %}
                N'est pas actifs
            {% endif %}
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
