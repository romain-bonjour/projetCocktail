{% extends 'base.html.twig' %}

{% block title %} {{ cocktail.label }} {% endblock %}

{% block body %}
    <h1>{{ cocktail.label }}</h1>
    <h5>{{ cocktail.description }}</h5>

    <a class="nav-link" href="{{ path('menu') }}">Retour</a>
{% endblock %}
